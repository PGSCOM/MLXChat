enum DefaultModel {
    /// Placeholder used until Fase 2 ships the model catalog / Hugging
    /// Face search picker. Any valid HF repo id works here — the engine
    /// doesn't consult a fixed registry.
    static let repoID = "mlx-community/Qwen2.5-0.5B-Instruct-4bit"
}
