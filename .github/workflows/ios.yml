name: Build unsigned IPA

on:
  push:
    branches: [main, master]
    tags: ["v*"]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  HOMEBREW_NO_AUTO_UPDATE: "1"

jobs:
  build:
    runs-on: macos-26
    timeout-minutes: 90

    steps:
      - uses: actions/checkout@v7

      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: "26.4"

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project
        run: xcodegen generate --spec project.yml

      - name: Cache SwiftPM checkouts
        uses: actions/cache@v6
        with:
          path: SourcePackages
          key: ${{ runner.os }}-spm-${{ hashFiles('project.yml') }}
          restore-keys: ${{ runner.os }}-spm-

      - name: Resolve package dependencies
        run: |
          xcodebuild -resolvePackageDependencies \
            -project Faro.xcodeproj \
            -clonedSourcePackagesDirPath "$PWD/SourcePackages" \
            -skipPackagePluginValidation

      - name: Cache DerivedData
        uses: actions/cache@v6
        with:
          path: DerivedData
          # Own prefix: a bare `derived-data-` also matched the test job's
          # simulator cache, whose Faro.app then got packaged as the IPA.
          key: ${{ runner.os }}-derived-data-build-${{ hashFiles('project.yml') }}-${{ github.run_id }}
          restore-keys: |
            ${{ runner.os }}-derived-data-build-${{ hashFiles('project.yml') }}-
            ${{ runner.os }}-derived-data-build-

      - name: Build (unsigned)
        run: |
          set -o pipefail
          xcodebuild build \
            -project Faro.xcodeproj \
            -scheme Faro \
            -configuration Release \
            -sdk iphoneos \
            -destination 'generic/platform=iOS' \
            -derivedDataPath "$PWD/DerivedData" \
            -clonedSourcePackagesDirPath "$PWD/SourcePackages" \
            -skipPackagePluginValidation \
            -skipMacroValidation \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGN_ENTITLEMENTS=""

      # Exact path, never a search: a restored DerivedData cache can also hold
      # a simulator build (Debug-iphonesimulator/Faro.app), and packaging that
      # one gives an .ipa that won't launch on a device.
      - name: Locate built app
        run: |
          APP_PATH=DerivedData/Build/Products/Release-iphoneos/Faro.app
          if [ ! -d "$APP_PATH" ]; then
            echo "::error::Faro.app not found at $APP_PATH"
            find DerivedData/Build/Products -maxdepth 2
            exit 1
          fi
          echo "APP_PATH=$APP_PATH" >> "$GITHUB_ENV"

      - name: Verify MLX Metal shaders were compiled
        run: |
          if [ -z "$(find "$APP_PATH" -name default.metallib 2>/dev/null)" ]; then
            echo "::error::default.metallib missing from Faro.app — MLX GPU ops would crash on device"
            exit 1
          fi

      - name: Package IPA
        run: |
          mkdir -p Payload
          cp -R "$APP_PATH" Payload/
          zip -qry Faro-unsigned.ipa Payload
          ls -lh Faro-unsigned.ipa

      - uses: actions/upload-artifact@v7
        with:
          name: Faro-unsigned-ipa
          path: Faro-unsigned.ipa
          if-no-files-found: error
          retention-days: 30

      - name: Attach to release
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release upload "${GITHUB_REF_NAME}" Faro-unsigned.ipa --clobber

  test:
    runs-on: macos-26
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v7

      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: "26.4"

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project
        run: xcodegen generate --spec project.yml

      - name: Cache SwiftPM checkouts
        uses: actions/cache@v6
        with:
          path: SourcePackages
          key: ${{ runner.os }}-spm-${{ hashFiles('project.yml') }}
          restore-keys: ${{ runner.os }}-spm-

      - name: Cache DerivedData
        uses: actions/cache@v6
        with:
          path: DerivedData
          key: ${{ runner.os }}-derived-data-test-${{ hashFiles('project.yml') }}-${{ github.run_id }}
          restore-keys: |
            ${{ runner.os }}-derived-data-test-${{ hashFiles('project.yml') }}-
            ${{ runner.os }}-derived-data-test-

      - name: Run unit tests
        run: |
          set -o pipefail
          xcodebuild test \
            -project Faro.xcodeproj \
            -scheme Faro \
            -destination 'platform=iOS Simulator,name=iPhone 17' \
            -derivedDataPath "$PWD/DerivedData" \
            -clonedSourcePackagesDirPath "$PWD/SourcePackages" \
            -skipPackagePluginValidation \
            -skipMacroValidation
